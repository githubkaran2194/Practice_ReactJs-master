// auth.js
export const authenticateUser = (username, password) => {
    // In a real application, you would perform authentication logic here
    // For simplicity, let's check against hardcoded values
    return username === 'karan' && password === '123';
  };
  